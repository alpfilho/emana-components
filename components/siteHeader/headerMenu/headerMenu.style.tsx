import styled from 'styled-components';

export const HmNav = styled.nav`
	position: relative;
	z-index: 110;
	width: auto;
	list-style: none;
	display: flex;
	flex-direction: row;
	align-items: center;
	justify-content: center;
	will-change: opacity, transform, color, background;
`;
