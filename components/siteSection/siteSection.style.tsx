import styled from 'styled-components';
import { Content, Container } from '../contentContainer';

export const SsSection = styled.section`
	width: 100%;

	${Container} {
		${Content} {
			padding-top: 92px;
			padding-bottom: 92px;
		}
	}
`;
